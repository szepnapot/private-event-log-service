# private-event-log-service


### Start and run tests
```bash
git clone git@github.com:szepnapot/private-event-log-service.git
cd private-event-log-service && python3 -m venv && source venv/bin/activate && pip3 install -r requirements.txt
pytest
```
